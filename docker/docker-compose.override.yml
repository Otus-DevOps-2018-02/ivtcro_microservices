version: "2.0"
services:
  ui:
    command: "puma --debug -w 2"
    volumes:
    - ${APP_PATH}/ui:/app

  post:
    volumes:
    - ${APP_PATH}/post-py:/app

  comment:
    command: "puma --debug -w 2"
    volumes:
    - ${APP_PATH}/comment:/app
